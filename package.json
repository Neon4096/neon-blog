{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:cfpagebuild": "git fetch --unshallow && vitepress build docs"
  },
  "devDependencies": {
    "vitepress": "^1.4.1"
  }
}
